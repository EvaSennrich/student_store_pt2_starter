const db = require("../db");

class Order {
  static async listOderForUser() {
    // method will return all orders that the authenticated user has created. Leave it empty for now except for some comments explaining what will be done.
  }

  static async createOrder() {
    //  method will take a user's order and store it in the database. Leave it empty for now except for some comments explaining what will be done.
  }
}

module.exports = Order;
